────────────────────────────────────────
Review prompt — html-gen 测试执行效率优化
────────────────────────────────────────

对 html-gen 项目 ~/CodeSpace/html-gen 审查测试执行效率优化方案。

聚焦:
- documents/test-speed-optimization-design-v1.0-20260819.md (commit 9565b8b)

背景:
- 全量 136 tests 实测 165.58s, 瓶颈 = 固定 sleep 53s + setUp 页面加载 sleep 75s
- 最小验证已实测: xdist -n 4 → 48.19s (-70%); 调低 sleep → -26%; WebDriverWait 替换 setUp → -38%; 组合单文件 -47%
- 目标: 三者组合全量 ~25-30s (-82%)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
检查项
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

S1 · 问题分析准确性
  □ 耗时构成量化正确 (sleep 53s + setUp 75s + 执行 35-40s)
  □ 最小验证数据真实且已实测 (非推测)

S2 · 方案 C (xdist 并行)
  □ 依赖合理性: pytest-xdist 仅 dev 依赖, 不违反运行时零依赖
  □ -n 4 与 10 核匹配, 已实测无资源竞争
  □ test_templates 生成 demos 与并行 worker 隔离性已验证

S3 · 方案 D (sleep 调低)
  □ 保守映射表合理 (0.3→0.15, 0.6→0.3 等; 小值不动)
  □ 逐文件验证 + 回退策略明确
  □ 未改变测试语义 (只改等待机制)

S4 · 方案 D' (WebDriverWait)
  □ 主元素选择器按页面类型 (table/knowledge/doc) 区分
  □ 超时兜底 5s 合理
  □ 事件驱动 vs 固定 sleep 的优势论证充分

S5 · 实施顺序
  □ 先 D' 后 D, 单线程验证 → 并行验证
  □ 验收标准: 单线程无 flaky + 并行 136 passed ~25-30s

S6 · Skills 沉淀
  □ 触发条件/方法论/方案/坑位完整
  □ speedup_sleeps.py 脚本设计 (dry-run/幂等/备份/白名单)
  □ skill 落位 skills/ 目录 (与项目 4 个现有 skill 并列)

S7 · 风险与文档
  □ flaky 风险缓解 (保守映射 + 连跑 2 次)
  □ AGENTS.md 测试治理节更新
  □ pytest.ini addopts 决策 (待确认项是否合理)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
产出
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 按治理规范逐项审查
2. 报告审查结果 (PASS / CONDITIONAL PASS / REJECT + 评分 + 修改意见编号)
3. 若 PASS → 提示切 dev role 实现 + skills 沉淀
